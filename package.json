{
  "name": "annuaire-alumni",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace=apps/web",
    "dev:cms": "npm run develop --workspace=apps/cms"
  },
  "dependencies": {
    "@tailwindcss/postcss": "4.2.0",
    "tailwindcss": "4.2.0"
  },
  "devDependencies": {
    "esbuild": "^0.27.3"
  }
}
